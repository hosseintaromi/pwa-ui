import { handleChange } from '@/components/shared/components/form/type';

type Props = {
  value: Date;
  errorMessage: string | undefined;
  name: string;
  handleChange: handleChange;
};
export default Props;
